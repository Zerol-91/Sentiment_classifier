services:
  - type: web
    name: sentiment-bot
    env: docker
    docker:
      dockerfile: Dockerfile.bot
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        fromSecret: true
      - key: API_URL
        value: https://sentiment-classifier-lox2.onrender.com
    healthCheckPath: /health